spring:
  datasource:
    url: jdbc:tc:mysql:8.0:///diet_management?TC_TMPFS=/testtmpfs:rw
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: root
    password: test
  flyway:
    enabled: true
    baseline-on-migrate: true
